apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "backend.fullname" . }}-collected-pv
  labels:
{{ include "backend.labels" . | indent 4 }}
spec:
  {{- with .Values.persistence.collected }}
  capacity:
    storage: {{ .size | quote }}
  accessModes: 
  - {{ .accessModes | quote }}
  storageClassName: {{ .storageClassName | quote }}
  hostPath:
    path: {{ .hostPath | quote }} 
  {{- end }}
  